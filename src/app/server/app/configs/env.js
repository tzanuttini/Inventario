const env = {
  listenOn: 'localhost',
  puerto: 3000,
  database: 'Lab4TPFinal',
  usuario: 'postgres',
  clave: 'postgres',
  host: 'localhost',
  port: 5432,
  dialect: 'postgres',
  max: 5,
  min: 0,
  idle: 30000,
  acquire: 60000,
  
};

module.exports = env;
